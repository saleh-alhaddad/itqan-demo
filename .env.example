DATABASE_URL="postgresql://itqan:itqan@localhost:5432/itqan_dev"
SESSION_COOKIE_NAME="itqan_session"

# Local Postgres 18 (Homebrew) is the default dev target; `docker compose up -d` starts an
# equivalent instance on 5433 if you prefer a container (change the port below to 5433).
